# StarBridge — LI.FI Widget v4 test

This build upgrades the LI.FI Widget to v4 and configures the EVM provider explicitly.

For this diagnostic build, no integrator fee is configured. Once routing is confirmed, add the LI.FI v4 `feeConfig` back and verify the StarBridge integrator in the LI.FI Partner Portal.
